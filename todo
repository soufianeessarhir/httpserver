- consider use 0 timeout when the there is active connection and wait infinetly when there is there is no active connection
- implemet timouts
- remove support for port only binding 
- if host header does not exest the server should responce by error
- need to remove errno when sending